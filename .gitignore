# ---------- 의존성 / 빌드 산출물 ----------
node_modules/
.next/
out/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ---------- 환경변수 ----------
# 실제 값은 절대 커밋하지 않습니다.
# .env 로 시작하는 모든 파일을 막고, 견본인 .env.example 만 예외로 허용합니다.
# (.env.secret 같은 임의의 이름을 만들어도 자동으로 차단됩니다)
.env*
!.env.example

# ---------- 자격증명 / 인증서 ----------
*.pem
*.key
*.p12
*.pfx
credentials.json
serviceAccount*.json

# ---------- 배포 ----------
.vercel/

# ---------- 테스트 / 로그 ----------
/coverage
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ---------- DB 덤프 ----------
dump/
*.bson

# ---------- 에디터 / OS ----------
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# humanize 작업 폴더 (원본 백업)
_workspace/
