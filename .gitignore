node_modules/
.next/
out/
build/
.DS_Store

# 환경변수 — 실제 값은 절대 커밋하지 않습니다.
# .env 로 시작하는 모든 파일을 막고, 견본인 .env.example 만 예외로 허용합니다.
# (.env.secret 같은 임의의 이름을 만들어도 자동으로 차단됩니다)
.env*
!.env.example

*.tsbuildinfo
next-env.d.ts
npm-debug.log*
